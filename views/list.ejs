<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Todo List</title>

</head>

<body>
    <div class="box">
        <h1>Todo List</h1>
    </div>
    <div class="box">

       <% ejes.forEach(task => { %>
<div class="todo-item">
    <input class="checkbox" type="checkbox">
    <p class="<%= task.priority === 'High' ? 'high-priority' : task.priority === 'Medium' ? 'medium-priority' : 'low-priority' %>">
        <%= task.task %>
    </p>

    <!-- EDIT (PUT) -->
    <form action="/edit/<%= task._id %>?_method=PUT" method="post" style="display:inline;">
        <input type="text" name="newTask" placeholder="Edit task">
        <button id="editButton" type="submit">Edit</button>
    </form>

    <!-- DELETE (DELETE) -->
    <form action="/delete/<%= task._id %>?_method=DELETE" method="post" style="display:inline;">
        <button id="deleteButton" type="submit">Delete</button>
    </form>
</div>
<% }) %>


                <form action="/" method="post" onsubmit="return validateForm()">
                    <input type="text" id="inputField" name="ele1" placeholder="Enter the task">
                    <select name="priority">
                        <option value="High">🔴High</option>
                        <option value="Medium">🟠Medium</option>
                        <option value="Low">🟢Low</option>
                    </select>
                    <button id="addButton" type="submit">ADD</button>
                </form>

    </div>


    </div>
    <script>
        function validateForm() {
            const input = document.getElementById("inputField").value.trim();
            if (!input) {
                alert('Input cannot be empty!');
                return false;
            }
            return true;
        }
    </script>
</body>

</html>